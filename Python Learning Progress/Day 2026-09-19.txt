PYTHON PRACTICE

Date / Data: 19.09.2026
Program / Program: Login System

GENERAL PLAN / PLAN OGÓLNY

1. What was done / Co zostało zrobione

2. What I learned / Czego się nauczyłem

3. Next steps / Następne kroki

4. What I still want to learn / Czego chcę się jeszcze nauczyć


---

DETAILED PLAN — ENGLISH

I. WHAT WAS DONE

- Expanded the basic login system with a separate logged-in user session.

- Created user_menu() for the menu displayed after successful login.

- Created user_session() for controlling the logged-in user's session.

- Connected the Main Menu with the User Menu.

- Added two ways to leave the user session:

    - logout and return to the Main Menu,

    - logout and exit the program.


- Added session_running to distinguish the user session from the main program state running.

- Divided the program into separate functions with specific responsibilities:

    - main_menu()

    - user_menu()

    - get_choice()

    - handle_menu_choice(choice)

    - register()

    - login()

    - check_login(login_username, login_password)

    - load_users()

    - user_session()


- Continued working with users.txt.

- Used the username:password format, with one user per line.

- Implemented loading users from the TXT file.

- Implemented splitting the file contents into individual users.

- Implemented checking entered credentials against the users loaded from the file.

- Initialized login_successful as False at the beginning of check_login().

- Tested successful login, failed login, entering the User Menu, logout, and program exit.

- Kept "w+" in register() intentionally for the current version. A new registration currently replaces the existing file contents.


II. WHAT I LEARNED

Program structure

How to divide a Python program into:

- Imports

- Variables and Constants

- Function Definitions

- Main Program / Main Logic


- How to divide a larger program into smaller functions.

- How each function can have a specific responsibility.

- How the main program controls the overall flow.


Functions

- How def defines a function.

- How to call a function.

- The difference between a parameter and an argument.

- A parameter is a local variable of the function.

- How arguments provide values to parameters.

- How return works.

- How a function can return True or False.

- How return values can control the flow between functions.

- What return user_session() means.


Variables and scope

- The difference between global and local variables.

- How scope works.

- That parameters are local variables.

- That assigning to a variable inside a function normally makes it local.

- That a global variable with the same name does not replace the local variable.

- What UnboundLocalError means.

- Why a local variable must have a value before it is read.

- Why login_successful = False is initialized at the beginning of check_login().

- That Python is case-sensitive.

- That a and A are two different variable names.


Loops and control flow

- How while works.

- How for works.

- How if, elif and else work.

- How break works.

- That break exits the nearest loop.

- That return exits the entire function.

- The difference between break and return.

- How running and session_running can represent different program states.


Input

- That input() always returns a string.

- Why menu choices are compared with values such as "1", "2" and "3".

- How input values are passed to functions.


TXT files

- How open() opens a file.

- How read() reads file contents.

- How write() writes data.

- How close() closes a file.

- The basic file modes:

    - "r"

    - "w"

    - "w+"

    - "a"

    - "a+"

    - "r+"


The difference between writing, overwriting and appending.

- How the file cursor works.

- How tell() shows the current file position.

- Why "a+" starts writing at the end of the file.

- Why "r" is used when only reading the users file.


String processing

- How strip() works.

- How split() works.

- How split("\n") separates users stored on different lines.

- How split(":") separates username and password.

- How method chaining works, for example: file.read().strip().split("\n").


Lists and data processing

- How file contents can be converted into a list.

- How to iterate through a list with for.

- How to process each stored user.

- How unpacking works: username, password = user.split(":").


Login logic

- How to collect login credentials.

- How to compare username and password.

- How to use a Boolean variable to represent login success.

- How successful login changes the program flow.

- How failed login returns the user to the Main Menu.

- How logout returns from the User Menu.

- How return values control the main program loop.


Testing and debugging

- How to test different program scenarios.

- Testing successful login.

- Testing failed login.

- Testing wrong username or password.

- Testing logout.

- Testing program exit.

- Understanding the difference between technical errors and logical/behavioral errors.

- Understanding that a program can run without crashing and still contain a logical defect.

- Thinking about different user actions and possible program states.


Naming and code organization

- The importance of meaningful variable names.

- Why session_running is clearer than running2.

- The importance of meaningful function names.

- Using Find and Replace in VS Code to change names consistently.


III. NEXT STEPS

- Add validation of Main Menu choices.
- Add validation of User Menu choices.
- Add username validation.
- Add password validation.

- Decide what should happen after invalid input.

- Define the actual functions of the User Menu.
- Implement the User Menu functionality step by step.

- Replace TXT storage with JSON.
- Learn the structure of JSON data.
- Save users in JSON.
- Load users from JSON.
- Adapt registration and login to JSON.

- Add a failed-login attempt limit.

- Add password hashing.

- Add password-change functionality.

- Later divide the project into separate modules.


IV. WHAT I STILL WANT TO LEARN

- JSON and JSON files.

- try / except.

- else and finally in exception handling.

- with open().

- FileNotFoundError and other file-related errors.

- Safe handling of invalid input.

- More advanced validation.

- Working with multiple users.

- Password hashing and basic password security.

- Modules and import.

- Organization of larger Python projects.

- More advanced file handling.

- More advanced data structures.

- Further debugging techniques.

- Making programs more robust against unexpected user input.



---

PLAN SZCZEGÓŁOWY — POLSKI

I. CO ZOSTAŁO ZROBIONE

- Rozbudowano podstawowy system logowania o osobną sesję zalogowanego użytkownika.

- Utworzono user_menu() dla menu wyświetlanego po poprawnym zalogowaniu.

- Utworzono user_session() do obsługi sesji zalogowanego użytkownika.

- Połączono Main Menu z User Menu.

- Dodano dwa sposoby opuszczenia sesji:

    - wylogowanie i powrót do Main Menu,

    - wylogowanie i zakończenie programu.


- Dodano session_running w celu rozdzielenia sesji użytkownika od głównego stanu programu running.

- Podzielono program na osobne funkcje o określonych zadaniach:

    - main_menu()

    - user_menu()

    - get_choice()

    - handle_menu_choice(choice)

    - register()

    - login()

    - check_login(login_username, login_password)

    - load_users()

    - user_session()


- Kontynuowano pracę z users.txt.

- Zastosowano format username:password, jeden użytkownik w jednej linii.

- Zaimplementowano odczytywanie użytkowników z pliku TXT.

- Zaimplementowano dzielenie zawartości pliku na poszczególnych użytkowników.

- Zaimplementowano sprawdzanie podanych danych logowania względem danych odczytanych z pliku.

- login_successful jest inicjalizowane jako False na początku check_login().

- Przetestowano poprawne logowanie, niepoprawne logowanie, wejście do User Menu, wylogowanie i zakończenie programu.

- Pozostawiono "w+" w register() celowo na obecnym etapie. Nowa rejestracja zastępuje obecnie zawartość pliku.


II. CZEGO SIĘ NAUCZYŁEM

Struktura programu

- Jak podzielić program Python na:

    - Imports

    - Variables and Constants

    - Function Definitions

    - Main Program / Main Logic


- Jak dzielić większy program na mniejsze funkcje.

- Jak przypisywać funkcjom konkretne zadania.

- Jak główny program steruje całym przebiegiem programu.


Funkcje

- Jak działa def.

- Jak wywoływać funkcję.

- Różnica między parametrem i argumentem.

- Parametr jest zmienną lokalną funkcji.

- Argument przekazuje wartość do parametru.

- Jak działa return.

- Jak funkcja może zwracać True lub False.

- Jak wartości zwracane przez funkcje mogą sterować przebiegiem programu.

- Co oznacza return user_session().


Zmienne i zakres

- Różnica między zmiennymi globalnymi i lokalnymi.

- Jak działa zakres zmiennych.

- Że parametry są zmiennymi lokalnymi.

- Że przypisanie wartości do zmiennej wewnątrz funkcji powoduje traktowanie jej jako lokalnej.

- Że globalna zmienna o tej samej nazwie nie zastępuje zmiennej lokalnej.

- Czym jest UnboundLocalError.

- Dlaczego zmienna lokalna musi mieć wartość przed jej odczytaniem.

- Dlaczego login_successful = False jest inicjalizowane na początku check_login().

- Że Python rozróżnia wielkość liter.

- Że a i A są różnymi nazwami zmiennych.


Pętle i sterowanie programem

- Jak działa while.

- Jak działa for.

- Jak działają if, elif i else.

- Jak działa break.

- Że break kończy najbliższą pętlę.

- Że return kończy całą funkcję.

- Różnica między break i return.

- Jak running i session_running mogą reprezentować różne stany programu.


Input

- Że input() zawsze zwraca string.

- Dlaczego wybory menu porównujemy z wartościami "1", "2", "3" itd.

- Jak przekazywać wartości pobrane przez input() do funkcji.


Praca z plikami TXT

- Jak open() otwiera plik.

- Jak read() odczytuje zawartość.

- Jak write() zapisuje dane.

- Jak close() zamyka plik.

- Podstawowe tryby plików:

    - "r"

    - "w"

    - "w+"

    - "a"

    - "a+"

    - "r+"


- Różnica między zapisem, nadpisywaniem i dopisywaniem.

- Jak działa kursor pliku.

- Jak tell() pokazuje aktualną pozycję kursora.

- Dlaczego "a+" ustawia pozycję zapisu na końcu pliku.

- Dlaczego do samego odczytu użytkowników używane jest "r".


Przetwarzanie tekstu

- Jak działa strip().

- Jak działa split().

- Jak split("\n") rozdziela użytkowników zapisanych w osobnych liniach.

- Jak split(":") rozdziela username i password.

- Jak działa łączenie metod, np.: file.read().strip().split("\n").


Listy i przetwarzanie danych

- Jak zawartość pliku zamienić na listę.

- Jak przechodzić po liście za pomocą for.

- Jak przetwarzać każdego użytkownika.

- Jak działa unpacking: username, password = user.split(":").


Logika logowania

- Jak pobierać dane logowania.

- Jak porównywać username i password.

- Jak używać zmiennej Boolean do określenia wyniku logowania.

- Jak poprawne logowanie zmienia przebieg programu.

- Jak niepoprawne logowanie powoduje powrót do Main Menu.

- Jak wylogowanie kończy User Menu.

- Jak wartości zwracane przez funkcje sterują główną pętlą programu.


Testowanie i debugowanie

- Jak testować różne scenariusze działania programu.

- Testowanie poprawnego logowania.

- Testowanie niepoprawnego logowania.

- Testowanie błędnej nazwy użytkownika lub hasła.

- Testowanie wylogowania.

- Testowanie zakończenia programu.

- Różnica między błędami technicznymi i błędami logicznymi/behawioralnymi.

- Zrozumienie, że program może działać bez błędu technicznego, a jednocześnie mieć błąd logiczny.

- Myślenie o różnych działaniach użytkownika i możliwych stanach programu.


Nazewnictwo i organizacja kodu

- Znaczenie używania opisowych nazw zmiennych.

- Dlaczego session_running jest lepszą nazwą niż running2.

- Znaczenie opisowych nazw funkcji.

- Korzystanie z Find and Replace w VS Code przy zmianie nazw.


III. NASTĘPNE KROKI

- Dodać walidację wyborów w Main Menu.
- Dodać walidację wyborów w User Menu.
- Dodać walidację nazwy użytkownika.
- Dodać walidację hasła.

- Ustalić, co ma się dziać po podaniu niepoprawnych danych.

- Określić funkcje dostępne w User Menu.
- Stopniowo zaimplementować funkcje User Menu.


- Zastąpić zapis TXT zapisem JSON.
- Nauczyć się struktury danych JSON.
- Zapisywać użytkowników w JSON.
- Odczytywać użytkowników z JSON.
- Dostosować rejestrację i logowanie do JSON.

- Dodać limit nieudanych prób logowania.

- Dodać haszowanie haseł.

- Dodać możliwość zmiany hasła.

- Później podzielić projekt na osobne moduły.


IV. CZEGO CHCĘ SIĘ JESZCZE NAUCZYĆ

- JSON i pliki JSON.

- try / except.

- else i finally w obsłudze wyjątków.

- with open().

- FileNotFoundError i inne błędy związane z plikami.

- Bezpieczne obsługiwanie niepoprawnych danych wejściowych.

- Bardziej zaawansowana walidacja.

- Praca z wieloma użytkownikami.

- Haszowanie haseł i podstawy bezpieczeństwa haseł.

- Moduły i import.

- Organizacja większych projektów Python.

- Bardziej zaawansowana praca z plikami.

- Bardziej zaawansowane struktury danych.

- Dalsze techniki debugowania.

- Tworzenie programów odporniejszych na nieoczekiwane dane użytkownika.