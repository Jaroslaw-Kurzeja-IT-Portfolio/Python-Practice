# różnica między wyświetlaniem (display) a zwracaniem (return)
# wyświetlanie - pojawienie się na ekranie
# zwracanie - oddane informacji po wykonaniu działania?
#
# podsumować możliwe działania: struktura danych c i d (zbiory i słowniki)
#
# podsumować czego się można nauczyć na tym kursie
#
# zdefiniować: metoda, argument i parametr, podprogram
# podprogram - (inaczej: funkcja, procedura) wydzielona część programu wykonująca jakieś operacje
# parametr podprogramu - nie jest zdefiniowany (nie jest znana wartość) a jedynie zadeklarowany
# argument podprogramu - (zwany też: parametr) jest zdefiniowany (znana wartość) /
# / wymagane (konieczne do wywołania podprogramu) lub opcjonalne
#
# zwraca =? wywołuje (nie mylić z 'wyświetla')
#
#
#
#
#
# Struktura kursu:
# (03) składnia: ...
# (04) typy danych: string, integer, float, bool, tuple...
# (05) operatory matematyczne: + - * /
# (06) operatory porównania: ==  !=  >  <  >=  <=
# (07) operatory logiczne: and or
# (08) działania na stringu: metody
# (09) instrukcje warunkowe: if, elif, else
# (10) pętl while: działa dopóki zwrócona wartość jest True
# (10) pętla for: działa określoną ilość razy
# (11) struktury danych:
# (11) lista (list) - [] - można ją modyfikować, wartości mogą się duplikować, elementy są uporządkowane
# (11) krotka (tuple) - () - nie da się modyfikować
# (11) zbiór (set) - set()  i {} - wartości nie mogą być zduplikowane, elementu niemutowalne, elementy nieuporządkowane
# (11) słownik (dictionary) - {} i {"": ""} - elementy mają klucze i wartości, klucze nie mogą się duplikować
# (12)
# (13)
# (14)
# (15)
#
#